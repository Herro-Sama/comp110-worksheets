Open Terminal
Generate words and noise
Store correct word
set player lives to 4

Check correct word()
CorrectCharacter equals zero
do a range for the number of characters in word
  if CorrectWord[range] == PlayerWord[range]
    CorrectCharacters += 1
Display correctcharacters against total characters

loop{
allow player to choose words
  if choice is correct 
    set terminal state to hacked
    end game
  elif player lives equals zero
    set terminal state to failed
    end game
  else player wrong
    Check correct word()
    remove a player life
}
